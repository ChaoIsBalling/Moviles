package com.example.engine;

import javax.sound.sampled.Clip;

/**
 * Interfaz que heredan los sound propios de cada motor
 */
public interface Sound {
    //No se ha implementado al final

    //String getName();
    //Object setClip(Clip c);
}
